"use client"

import api from "util/api/api";
import notify from "util/notify/notify";
import { useForm } from '@mantine/form';

export default function EditReviewModal({review, book}) {
	const submitEdit = async () => {

	}

	return (
		<div>
			Form go here
		</div>
	)
}
